# Firebase Web API key (client-side, dùng cho auth.js khi chạy Flask local).
# Lấy từ Firebase Console > Project settings > Web app config.
# Copy file này thành .env rồi điền giá trị thật:  cp .env.example .env
FIREBASE_API_KEY=
